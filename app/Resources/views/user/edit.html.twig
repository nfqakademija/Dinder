{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-12 col-xs-12 col-sm-12">
            <h1 class="title">Manage your profile information</h1>
            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <button type="submit" class="btn btn-default">Update My Info</button>
            {{ form_end(edit_form) }}
        </div>
    </div>
</div>
{% endblock %}
